# Examples of (in)valid NetCDF datasets

This folder contains three minimal CDL descriptions of NetCDF-datasets. One CDL-
description is valid with respect to the interface definition of `pf_passice`,
 while the remaining two are invalid (PATH to pf_passive.yaml).

These CDL-files can be used to generate nc-files by use of the Linux tool
`ncgen`.

(mini tutorial)